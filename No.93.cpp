/*
以空间换时间，节省开发时间
虽然有点偷工减料……不管方法如何，解题成功就可以了

3.13 输出100以内的所有素数	语言要求：	C++
编写一个程序，找出1~100中的所有素数。

输入输出格式要求：
要求每个数后面都要输出逗号，
请不要输出其他字符
例如：
输出：2,3,5,

*/
#include<stdio.h>
int main()
{
	int i;	
	int num[101] = { 0 };
	for (i = 1; i < 101; i++)		//数组初始化
		num[i] = 1;
	for (i = 1; i < 101; i++)
		if (i % 2 == 0 || i % 3 == 0 || i % 5 == 0 || i % 7 == 0)
			num[i] = 0;
	printf("2,3,5,7,");
	for (i = 10; i < 101; i++)
		if (num[i] != 0)
			printf("%d,", i);
	printf("\n");
	getchar();

	return 0;
}
